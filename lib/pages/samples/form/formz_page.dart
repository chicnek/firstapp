import 'package:flutter/material.dart';

class FormzPage extends StatefulWidget {
  const FormzPage({Key? key}) : super(key: key);

  @override
  _FormzPageState createState() => _FormzPageState();
}

class _FormzPageState extends State<FormzPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Formz pkgs"),
      ),
      body: Text("..."),
    );
  }
}
